'use client';import { useRef } from 'react';export default function CameraUpload({onFiles}:{onFiles:(files:FileList)=>void}){const ref=useRef<HTMLInputElement>(null);return(<><button className='btn' onClick={()=>ref.current?.click()}><img src='/camera-icon.svg' alt='camera' width={18} height={18} style={{verticalAlign:'middle',marginRight:8}}/>Caméra</button><input ref={ref} type='file' accept='image/*' capture='environment' multiple hidden onChange={(e)=>{if(e.target.files)onFiles(e.target.files);}}/></>);}
